if BUILD_ERCOLORSPACE
   ercolorspace_SUBDIR=ercolorspace
endif

SUBDIRS = $(ercolorspace_SUBDIR) videorepair scream
DIST_SUBDIRS = $(SUBDIRS) # needed since we are doing a out of tree build.

-include $(top_srcdir)/git.mk
